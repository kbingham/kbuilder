
ifneq ($(ARCH),aarch64)
CROSS_COMPILE?=aarch64-linux-gnu-
CMAKE_TOOLCHAIN_FILE=$(BUILDSYS_COMMON)/cmake/ubuntu-aarch64-linux-gnu.cmake
MESONFLAGS+=--cross-file=$(BUILDSYS_COMMON)/meson/aarch64-linux-gnu
endif

LINUX_DEFAULT_TARGET?=linux-Image
